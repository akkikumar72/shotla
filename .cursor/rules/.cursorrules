---
description: Use Bun instead of Node.js, npm, pnpm, or vite.
globs: "*.ts, *.tsx, *.html, *.css, *.js, *.jsx, package.json"
alwaysApply: false
---

In this codebase, always use Bun as the runtime and toolchain. Do not use Node.js, npm, pnpm, yarn, vite, or related tools.

- Use `bun <file>` to run scripts instead of `node <file>` or `ts-node <file>`.
- Use `bun test` for all tests instead of `jest`, `vitest`, or other runners.
- Use `bun build <entry>` for building instead of `webpack` or `esbuild`.
- Use `bun install` for dependency management. Do not use `npm install`, `yarn install`, or `pnpm install`.
- Use `bun run <script>` for running package scripts.
- Bun loads `.env` files automatically; do not use `dotenv`.

## APIs

- Use `Bun.serve()` for HTTP servers, WebSockets, HTTPS, and routing. Do not use `express`.
- Use `bun:sqlite` for SQLite databases. Do not use `better-sqlite3`.
- Use `Bun.redis` for Redis. Do not use `ioredis`.
- Use `Bun.sql` for Postgres. Do not use `pg` or `postgres.js`.
- Use the built-in `WebSocket` API. Do not use `ws`.
- Prefer `Bun.file` for file operations over `node:fs`.
- Use Bun's shell syntax (`Bun.$\`command\``) instead of `execa`.

